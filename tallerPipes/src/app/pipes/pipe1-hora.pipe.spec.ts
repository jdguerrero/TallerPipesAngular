import { Pipe1HoraPipe } from './pipe1-hora.pipe';

describe('Pipe1HoraPipe', () => {
  it('create an instance', () => {
    const pipe = new Pipe1HoraPipe();
    expect(pipe).toBeTruthy();
  });
});
