import { Pipe5MismoMesPipe } from './pipe5-mismo-mes.pipe';

describe('Pipe5MismoMesPipe', () => {
  it('create an instance', () => {
    const pipe = new Pipe5MismoMesPipe();
    expect(pipe).toBeTruthy();
  });
});
