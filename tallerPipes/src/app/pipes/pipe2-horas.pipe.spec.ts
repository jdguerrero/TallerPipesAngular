import { Pipe2HorasPipe } from './pipe2-horas.pipe';

describe('Pipe2HorasPipe', () => {
  it('create an instance', () => {
    const pipe = new Pipe2HorasPipe();
    expect(pipe).toBeTruthy();
  });
});
