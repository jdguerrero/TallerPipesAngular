import { PipeAnnioPipe } from './pipe-annio.pipe';

describe('PipeAnnioPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeAnnioPipe();
    expect(pipe).toBeTruthy();
  });
});
